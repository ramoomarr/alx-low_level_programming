#include <stdio.h>

/**
 * main - This is the main function.
 * 
 * Description: This program prints the alphabet in lowercase
 *
 * Return: Always 0.
 */
int main(void)
{
	char ch;

	ch = "a";


	while (ch < = "z")
	{
		putchar(ch);
		ch++;
	}

	putchar('\n');
	return (0);
}
